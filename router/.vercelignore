# The database is a hosted service now, so nothing here needs to carry data.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.git/
tests/
.playwright-mcp/

# The old SQLite index, kept locally as the migration's source. Shipping it
# would upload 1.3 MB the deployment never opens.
router.db
router.db-wal
router.db-shm

# Local only, never leaves the machine.
app/sources.local.yaml
